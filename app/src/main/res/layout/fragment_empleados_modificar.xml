<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".EmpleadosModificarFragment">
    <LinearLayout
        android:id="@+id/area_busqueda"
        android:layout_marginBottom="@dimen/line_height"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title"
        android:orientation="horizontal">

        <SearchView
            android:paddingLeft="15dp"
            android:paddingRight="15dp"
            android:queryHint="Buscar"
            android:id="@+id/search_empleado"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <ListView
        android:layout_below="@+id/area_busqueda"
        android:id="@+id/listViewEmpleadosModificar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>